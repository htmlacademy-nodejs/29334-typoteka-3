"use strict";

const {articlesRoutes} = require(`./articles`);
const {mainRoutes} = require(`./main`);
const {myRoutes} = require(`./my`);

module.exports = {
  articlesRoutes,
  mainRoutes,
  myRoutes,
};
